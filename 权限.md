## Linux系统权限

### 1. 超级管理员

- #### linux系统的超级管理员用户是root

### 2. su指令

- #### 可以切换用户的语法
    ```bash
    sudo su - root
    ```

- #### -表示切换之后加载环境，建议加上
- #### 用户可以省略，默认切换到root
  
### 3. sudo命令

- #### 可以让一条普通的指令带上root权限
- #### 语法
    ```bash
    sudo 普通指令
    ````